---
import slug from "slug";

import type { FilmsResponse } from "../pocketbase-types";
type Props={
  film: FilmsResponse;
}
const film = Astro.props.film;
---
<a href={`/films/${film.id}-${slug(film.titre)}`}>
  {film.titre}

