---
import slug from "slug";

import type { FilmsResponse } from "../pocketbase-types";
import LinkFilms from "./LinkFilms.astro";
type Props={
  films: FilmsResponse[];
}
const films = Astro.props.films;
---
{films.map((film) => (
  <LinkFilms film={film}/>
))}

